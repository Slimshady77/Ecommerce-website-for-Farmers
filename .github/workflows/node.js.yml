name: Full CI/CD Pipeline

on:
  push:
    branches: [deploy-2]

jobs:
  build-and-deploy:
    runs-on: self-hosted
    strategy:
      matrix:
        node-version: [18.x]

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}

    - name: Install dependencies and build backend
      working-directory: backend
      run: |
        npm install
        npm start

    - name: Deploy backend
      working-directory: backend
      run: |
        npm start  # Assuming your start script handles the deployment
